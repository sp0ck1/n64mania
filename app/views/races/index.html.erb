<h1>Listing Races</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date (estimated)</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @races.each do |race| %>
      <tr>
        <td><%= link_to Game.where(id: race.game_id).first.name, race %></td>
        <td><%= race.date %></td>
    

      </tr>
    <% end %>
  </tbody>
</table>

<br>