<table>
 <tr>
  <th>ID</th>
  <th>Name</th>
  
 </tr>
 <% @games.each do |game| %>
 <% j = game.id %>
 <tr>
  <td><%= j %></td>
  <td><%= link_to game.name, game_path(game) %></td>
  
 </tr>
 <% end %>
</table>